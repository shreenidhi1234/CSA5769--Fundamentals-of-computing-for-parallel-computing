#include <stdio.h>
int main()
{
	int i,n,sum=0;
	printf (" sum of even cube numbers");
	scanf ("%d",& n);
	for (i=0;i<=n;i++)
	{
		if(n%2==0)
		{
			sum+=i*i*i;
		}
		}
		printf ("%d",sum);
		return 0;
	 }
